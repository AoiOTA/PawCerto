# Corrected UMI training experiment

This plan was fixed before any corrected-signal training result, on 2026-09-12. It follows the completed stock solver-force shadow and three physics-mapping fixes. A real 4096-environment, one-iteration integration check must first establish runtime correctness and measured resource use. It is a smoke check, not part of the learned weight chain.

The integration check passed on both the preserved latest release and the isolated fixed Lab `develop@412fb31b30ee605b4ffec4327436fc0fe53281d8` + Sim 6.1 combination. The latter is selected for this experiment after its native interfaces, upstream body-property application, three free-dynamics samples, reward freshness and 4096-environment PPO iteration passed, with independent review. See [version validation](../outputs/isaac/lab-develop-sim610-validation/result.md). The user explicitly permits the latest beta release as fallback: if substantive compatibility problems emerge, use the already exercised `v3.0.0-beta2.patch1` + Sim 6.0.1 combination. Do not migrate a running weight chain silently between engines. Actual source, package versions and import paths are saved in configuration provenance.

The first training run starts from random weights with seed 0 and a fixed primary endpoint of **4000 iterations**. It uses the released `reference/checkpoints/tossing/ours/config.json` (SHA-256 `fbe6c6a38f3abffeeb77e15d767032bd8bf3b8611b1875496ce561404a1b68eb`), original Go2+ARX5, published tossing pool, 17-second task, 132/261 actor/critic inputs, eight previews, 4096 environments, 24 transitions per environment, and the original 64 PPO epochs / four minibatches. The nominal weight chain contains 393216000 transitions. Intermediate checkpoints are retained every 500 iterations; they do not replace the preselected primary endpoint after inspecting results.

The EMD input is explicitly `reconstructed-solver`. The original EMD formula and weight remain unchanged. Body properties are applied to every rigid link, the original angular-speed limit is converted to the Lab interface's units, and joint-friction randomization uses the verified legacy coefficient API. Each checkpoint must retain these semantics in configuration metadata. No proxy checkpoint optimizer is resumed into this experiment. Historical proxy runs remain available but are not a single-factor causal control for the new signal because physical mappings also changed.

MuJoCo fixed-weight checks use the same `sample(16, 2027)` at initialization, iteration 500 and the primary endpoint, each requested for 17 seconds. **Before any corrected long training or its initialization evaluation, the baseline was explicitly updated to MuJoCo 3.13.0** following the user's version request and the completed [48-case regression](../outputs/mujoco/version-3.13-regression/README.md). Use the existing isolated `third_party/mujoco-3.13-probe` environment and the newly executed 3.13 official-policy reference; retain 3.8 results and environment as historical evidence. All 48 cases completed with unchanged sampled support/contact counts and small numerical differences. This extends the earlier two-case probe and does not guarantee arbitrary-policy stability. Numerical warnings, resets and truncated prefixes remain explicit failures. The final Isaac Lab evaluation uses the author's completed-episode protocol, seed 2027, 250 environments and the latest 500 completed episodes after the original stopping rule, with current runtime metadata also recorded for the official reference.

Report position and orientation errors together with inversion, actual ground support, nonfoot contacts, timeouts and early-termination duration. A finite loss or completed training budget is insufficient. Results are drawn from the published training pool; this is not an independent trajectory holdout or paper-level statistical replication. Intermediate results can motivate a diagnosed correction, but an interrupted or changed run must retain its identity and failure record.

If the implementation remains valid and seed 0 demonstrates useful learned behavior, repeat the same fixed budget for seeds 1 and 2 to assess training variability. Do not count official weights or historical proxy seeds toward these repeats. If it does not, first use the measured learning/behavior failure to decide the smallest justified correction; do not launch repetitions of a known invalid implementation or adjust acceptance thresholds to pass.

The dedicated GPU operator owns launch, logs, checkpoint completion and runtime failures. CPU evaluation and independent review continue alongside that run. Actual throughput and remaining resource estimates come from the integration check and sustained run; no training-duration promise is inferred from the instrumented single-environment shadow.

## Execution identity

The initial main-code run is `runs/umi_reconstructed_solver_seed0_4096`. An external SIGTERM ended it after iteration 29; its sender is unknown. Its last complete checkpoint is iteration 0. The 2,850,816 unsaved transitions are retained as discarded work and do not count toward the effective 4000-iteration budget. The authorized same-engine recovery uses that checkpoint and a separate `runs/umi_reconstructed_solver_seed0_4096_resume` output. Optimizer, RNG and task state are restored; physics resets, so this is not an uninterrupted-trajectory replay. A brief intervening restart in the original directory was deliberately stopped to correct the output path; its records are also retained. The original initialization checkpoint matches its archive and CPU preflight hash.

The completed [MuJoCo initialization baseline](../outputs/mujoco/umi_reconstructed_solver_seed0_seed2027/model_0/README.md) contains all 16 full cases. Mean EE position/orientation errors are 246.695 mm / 0.540687 rad, with no inversion or numerical failure. This is random initialization, not a learned-control result. No iteration 500 or 4000 was reached in that recovery; future nodes must come from the final selected chain below.

The recovery subsequently completed 364 iterations and failed during iteration 365 at the post-step gyroscopic velocity-domain guard (actual exit 1). No model 500 exists. This is an internal supported-domain failure, separate from the earlier external SIGTERM. Subsequent native samples and source analysis determined the DOF/gyro correction below; thresholds and original physical limits remain unchanged. That recovery supplied no completed corrected weight chain; the separately initialized final chain below subsequently completed the fixed endpoint of 4000.

An intermediate, superseded proposal for a gyro-only rerun selected the existing `--save-every 100` option to preserve completed updates more often after the observed loss before checkpoint 500. This changes storage frequency only; evaluation remains at 0/500/4000 and the primary endpoint remains 4000. That proposal named a new random seed-0 chain in `runs/umi_reconstructed_solver_gyro_seed0_4096`, with `reconstruction_revision=stock-physx-gyro-clamp-v1`; the older optimizer could not cross that contract change. This proposed gyro-only run was superseded before long training by the complete velocity correction below.

## Selected corrected execution after velocity-branch coverage

The final selected new chain is `runs/umi_reconstructed_solver_velocity_seed0_4096`: random seed 0, no weights/resume, 4000 iterations, save every 100; evaluation remains 0/500/4000. It uses `physics_revision=umi-physics-mapping-v3` and `reconstruction_revision=stock-physx-velocity-clamps-v2`. The physical mappings now include original URDF joint speed limits and contact/rest offsets. The unified FD velocity-clamp correction passed all 450 components of three native positive/negative/unsaturated samples on actual CUDA; 38 CPU tests and independent review passed. A real main-code 4096-environment iteration completed with exit 0, 98,304 transitions, 256 Adam updates and finite checkpoint/metrics. Its weights remain separate from this new chain.

The new chain ran from 2026-09-11 19:09 to 21:16 UTC and completed all 4000 iterations, 393216000 transitions, with actual exit 0. Its [execution record](../runs/umi_reconstructed_solver_velocity_seed0_4096/execution.json) reports 7663.493 seconds total process wall time and a sampled GPU-process memory peak of 6254 MiB. The [final checkpoint check](../runs/umi_reconstructed_solver_velocity_seed0_4096/model4000-validation.json) verified 73 finite tensors, Adam step 1024000, the current runtime contract and unchanged training sources. Behavioral evaluation remains separate from these execution checks.

The [exact invocation](../runs/umi_reconstructed_solver_velocity_seed0_4096/execution-plan.json) uses the recorded exception-capture copy of the main CLI; the retained diff adds failure-state capture and leaves successful training unchanged. The [new initialization identity check](../outputs/mujoco/umi_reconstructed_solver_velocity_seed0_seed2027/model_0/README.md) establishes that all 17 policy tensors, inference configuration, source/model/trajectory hashes and 16 sampled targets match the existing MuJoCo baseline. That baseline is explicitly reused without new simulation. This inference identity does not permit optimizer recovery across the changed physics contract.

This is an explicitly reconstructed-signal learning experiment. The original EMD formula is retained, but near-zero force residuals can change its flying branch; original Gym low-speed solver-free checks returned exact zero. The earlier high-speed complete 6D diagnostic also retains three angular failures. Neither limit is relabeled as PASS by the later DOF validation or successful PPO integration. The experiment can establish measured learning and transfer under its recorded implementation; it cannot establish pointwise original sensor/reward equivalence or full paper reproduction.
