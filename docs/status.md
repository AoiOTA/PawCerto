# Current development status

Current priority: preserve the completed AS2 general-EE candidate and resolve its remaining control-execution differences from evidence. A 20-solve, zero-optimizer replay now isolates native Piper velocity enforcement as a cause of the first-tick arm response difference: raising only Lab's six arm limits from 5 to 1000 rad/s produces peaks close to MuJoCo. This does not establish the full numerical-failure cause or a transferable controller; see the [bounded causal result](as2-general-ee-learning-result.md#首步速度约束隔离结果). RoboDuet has completed its original 50000-update budget and final evaluation with a negative stable-control result.
The earlier AS2 EDU/Piper H candidates also ended with failed stable-control
and transfer results; the new budget is explicitly separate. DeepWBC's bounded 21-update/export/500-step path and source
delivery are complete; its learning outcome remains unproved. Hardware execution
remains outside scope. UniFP and Learning Force Control now have bounded Lab
integration at the evidence levels below; full force-control
learning and the remaining method integrations are unfinished.
See [the updated sequence](project-plan.md#updated-execution-priority-2026-09-12).

The [rail-mounted AS2 adaptation](as2-pretraining-family.md) completed its separately authorized 1024×24×1000 updates in 55.37 minutes and all model0/final×nominal/loaded×Lab/Mu evaluations. Nominal Lab EE tracking/support regressed, and every MuJoCo case failed numerically; this did not establish stable transferred control. The old 4000-update experiment remains separate.

On 2026-09-14 the user approved the [AS2 general-EE candidate](as2-general-ee-pretraining.md): fresh seed0 actor/critic/optimizer, 1024 environments ×24 rollout steps, at most 1000 updates or 5400 training-loop seconds, whichever boundary arrives first. The fixed model0/final ×8 tasks ×nominal/20 kg-body+2 kg-payload ×Lab/MuJoCo evaluation has 64 base cases. The initial progress commit ad83578 was pushed before launch as requested. After an experiment-recorder sensor lookup failed before any completed optimizer update, a local correction resumed the same fresh initialization and entered actual updates on 2026-09-14 at 09:05:35. The startup audit verified 45 updates, finite live coverage for 22 tracked environments and the original budget with 27 seconds conservatively deducted; the full [1000-update/64-case endpoint](as2-general-ee-learning-result.md) is now complete. Final nominal Lab shows partial arm/forward behavior but 3/8 inversions; loaded Lab has 7/8 inversions and worse tracking/support. Final MuJoCo fails 16/16 before the movement schedule. The fixed 22-env training coverage is complete without proving stable full-load control. See the linked AS2 records for all failures and scope limits. No learning result is inferred from the completed 22-asset consumption, task generation or COM reconstruction.


RoboDuet's seed-0, 4096-environment learning experiment completed exactly
50000 updates, 4,915,200,000 transitions and 39,999 arm updates after an initialization-memory repair. The first two attempts
did not produce updates: one was killed by kernel OOM and another coincided
with systemd-oomd terminating the desktop application's shared process group.
The repaired run preserves collision-geometry sharing and runs in an independent
service. Fixed physical evaluation of checkpoints 0/1600 has now completed:
2/9 versus 0/9 full 20-second cases, with worse same-prefix leg tracking at 1600.
Every early failure triggers body height, with no inversion in these prefixes.
Both policies use released-arm `official_play`; this is not a causal Stage 1
ablation. The [10001 stage-boundary evaluation](roboduet-model10001-evaluation.md)
also completed with 9/9 low-height terminations at 0.10–0.40 seconds. On the
three-model common prefixes, EE position and yaw tracking regressed despite
smaller aggregate linear-velocity error. The checkpoint still has zero arm
updates. The [final model50000 evaluation](roboduet-model50000-evaluation.md) now completes the original experiment: 0/9 full 20-second cases, all nine height terminations within 0.10–0.28 seconds. Final weights/four optimizer states are finite and the five exported modules match every saved real policy input exactly; these checks do not establish learned control. Per-case action-clipping fractions are 89.8–96.3%. The stop boundary is reached without automatically adding updates or tuning. See also the [early physical readout and video](roboduet-evaluation-result.md) and [stage-boundary comparison](roboduet-model10001-evaluation.md).

DeepWBC's corrected complete terrain run finished 21 updates and a fixed 500-step physical evaluation, with contact in all 500 samples. EE RMSE was 0.47936 m and forward-velocity MAE 0.49878 m/s; one signed-roll failure was followed by an unfinished 455-sample episode. Independent installed-package and TorchScript consumers matched checkpoint actions on all actual observations within the recorded tolerance. This is bounded execution and export evidence, not learning success. The earlier zero-contact/freefall run is excluded from physical-task conclusions. The six-axis foot signal remains approximate on official, unmodified Isaac Lab/PhysX. See [the final corrected-terrain report](experiments/deepwbc-reconstructed-runtime-20260912.md), [training](deepwbc-training.md) and [evaluation/export](deepwbc-evaluation.md).

The AS2/Piper H fresh seed-0 candidate completed 4096 environments × 24 transitions × 4000 updates (393,216,000 transitions), and its service exited 0. In the final paired Lab evaluation, mean EE position/orientation error improved from 0.510769 m / 1.625607 rad to 0.119318 m / 0.363301 rad, but 7/16 cases inverted and ground support deteriorated. Final MuJoCo fixed16 transfer had 16/16 BADQACC failures and zero complete cases. Exported actor weights matched, but the verifier and independent exported-policy consumer failed numerically; package consumption did not pass. The one-candidate experiment is complete with a negative stable-control/transfer outcome. No further AS2 experiments or parameter changes follow automatically. The [final report](as2-umi-learning-result.md) retains all model 0/500/4000 outcomes and the failed export consumers. The earlier OOM, invalid inverted-initial-pose update and 601-row equivalent shared-geometry repair remain preserved. Installation/TCP values are nominal assumptions. See [the completed protocol](as2-umi-learning-plan.md), [geometry repair](as2-piper-isaac.md) and [asset assumptions](as2-piper-assets.md).

UniFP on B2/Z1 completed 20 updates plus one independent-process resume update.
The original resume process saved model 21 and then exited 1 in the boundary
probe; a separate zero-update repair probe exited 0 with strict stage flags
false/true/true at global steps 192000/192001/192002. The unique model-21
500-step evaluation exited 0, with all transitions finite and TorchScript
actually driving the robot, one roll reset and mean EE error 0.458129 m.
Independent CPU consumption of all real inputs matched checkpoint outputs
exactly (versus saved GPU actions/estimates, maximum differences 4.17e-7/1.49e-7).
Training missed the `gripperMover` rigid-property overrides; evaluation used
the repaired runtime, so training/evaluation physics are not claimed identical.
This is bounded integration, not learned force control. See [runtime evidence](unifp-lab-runtime.md)
and [evaluation/export](unifp-evaluation.md).

Learning Force Control on B1/Z1 now has [official Lab integration](learning-force-control.md)
in addition to the historical 181-blob-verified CPU source/checkpoint delivery.
The 16-environment, 48-step, 20-update run exited 0; independent resume saved
model 21 at 16,128 training transitions before a post-save force-probe failure
exited 1. The inference-context repair was exercised separately with zero
optimizer updates. The corrected [500-step, 16-environment evaluation](learning-force-control-evaluation.md)
exited 0 with all 8,000 transitions finite and actually JIT-driven, four
body-orientation resets and exact device-side checkpoint/JIT parity. Position-
mode EE error was 0.821928 m; force-mode XYZ component RMS was 40.852856 N
(48.492836 N for constrained force-mode samples). These forces are known applied
spring/external forces, not independent wrist/contact measurements. Independent
CPU consumption of all 8,000 histories matched CPU checkpoint outputs exactly
and saved GPU outputs within the combined tolerance. The first evaluation
failed during reset before any JIT consumption because the evaluator mapped
velocity limits to nonexistent source keys; the corrected attempt completed
the missing evaluation without another optimizer update. Both failures remain
preserved. The bounded assignment is closed without a learning-success claim. The fixed source ABI retains 19 outputs and native
position drives; computed source PD tensors are not submitted drive efforts.
Original Gym lookup `gripperStator=-1` measured the last body, `gripperMover`;
the port binds it explicitly while applying force to `link06`.
Derived-only xacro/duplicate-material repairs and upstream license provenance
now support the runtime port. Anonymous W&B lookup did not obtain author
weights. Neither bounded updates nor a recovered probe establishes learned
force control, exact cross-engine physics or hardware acceptance.


ReLIC's [optional original PLAY runtime](relic-external-runtime.md) completed
500 finite policy steps with one environment and zero optimizer updates,
eight undesired-ground-contact terminations and no timeouts. Actual 84→12
actor inputs/outputs, original 19-joint Spot and 25 body states were recorded;
all seven arm and selected-leg-three target overwrites matched source buffers
exactly. All 500 real inputs passed independent CPU replay at atol/rtol=1e-5
(maximum error 5.722e-6). Measured pre-reset tracking includes all terminal rows:
arm component RMSE 0.08834 rad, active selected-leg component RMSE 0.30100 rad
(404 steps), base COM XY velocity vector RMS error 0.90853 m/s and yaw-rate
RMSE 0.89630 rad/s. All four leg selections occurred. These are physical
tracking results, not stable whole-body performance or training reproduction.

The current `(1,25,1)` normal-contact scalar signal is not established equivalent
to historical vectors/total contact force. PLAY inherits Phase1 configuration,
but the supplied weights' training stage/seed/update count remain unknown.
Run 001's consumed steps are unknown; run 002 advanced physics before a contact-
axis failure; run 003 completed the requested window. External migrations and
all failures remain retained. The public package contains authored interoperability
only; noncommercial source/assets/models/license stay external. The earlier
[three-synthetic-input PT/ONNX comparison](relic-external-export.md) remains
separate CPU evidence.


The [approved continuation plan](project-plan.md) starts from the completed
three-seed UMI baseline. On 2026-09-12 the user selected seed-matched tracking
preservation and correction of uncontrolled behavior, with controllable calf
or other task-appropriate support allowed. A nonfoot contact count is not by
itself a failure. Optional 5 ms contact/execution diagnostics and grouped data
splits are implemented; isolated installation and training/save/resume entry
checks have completed. These
results are separate from the completed baseline below.
The corrected-inertia, 71-train candidate completed 4000 updates and the UMI
research reproduction stage. Tracking is comparable to the old baseline; the
user accepts the small mean differences for continued research publication.
The frozen-model test completed 14/15 trajectories; test ID 1 inverted, had
head-ground impact and ended in BADQACC. No complete 15-case test mean or
universal stability is claimed. See
[the current split-trained result](umi-source-inertia-result.md). KMA's independent release has completed as
[v0.2.9](https://github.com/AoiOTA/Kiss-My-Agent/releases/tag/v0.2.9): candidate
CI, public archive, isolated installation/upgrade, and fresh-session Skill
discovery passed. The user explicitly keeps the local development installation
at base version `0.2.8`; it has since received a local guidance development update.
KMA development remains separate from PawCerto's researcher dependencies and
public source deliverables.

Acceptance clarification from the user on 2026-09-12: exact numerical equality between Lab and Gym is not required. The current objective is a working UMI port with preserved method semantics, real Lab learning, fixed-policy MuJoCo validation and honest performance/failure reporting. Engine residuals remain diagnostic limitations; they do not alone make the project incomplete. Deliberate physical configuration changes, including the relaxed joint-speed limit, remain explicit.

The [current UMI closing report](umi-current-result.md) collects the completed first loop. Future defaults now use Conda for both Lab and MuJoCo, with real frozen-policy migration comparisons and independent review completed; historical training remains attributed to its original environments. The [executed contact diagnosis](umi-contact-next-step.md) preserves the original proposal and adds the completed 5 ms, ground/free, and warmup-state intervention results.

The new diagnostics found no action-delay or PD implementation error. All three
MuJoCo trace-on/off pairs retained exact endpoint arrays and JSON. The matching
Lab case remained supported without the head-contact sequence. Removing ground
contact reduced the first zero-torque, 5 ms cross-engine joint-speed difference
from 5.6033 rad/s to 1.488e-7 rad/s, identifying contact response as the dominant
initial difference. Replacing the MuJoCo warmup pose/velocity with the actual
Lab PD-warmup state did not eliminate the later head contact, so the
initialization-only direction was stopped. This is a negative intervention
result, not a behavior repair. That initialization-only branch is closed. A subsequent nonzero-torque
comparison identified incorrect imported USD principal inertia axes. The
source-tensor correction passed native validation; one new random-seed-0,
71-train, 4096 by 24 by 4000 candidate completed using an explicitly selected
corrected USD. Old assets, policies and results remain distinct. The frozen
candidate is proceeding as a research preview with the known instability
disclosed; the physics correction does not establish universal stability.

The [grouped data implementation](umi-data-split.md) selects 71 train, 15
validation and 15 test trajectories. It preserves the original source and
full-pool default, reads training identity from actual checkpoint metadata,
and does not qualify old full-pool or actor-only weights as held out. The
source contains no recording IDs, limiting recording-level independence
claims. Validation entrypoint checks and a fresh-environment 16-environment
training/save/resume check using the 71 train IDs have run. The latter completed
three updates: one initial update followed by two additional resume updates,
one more than intended because the operator misread the additional-iteration
argument. Execution is stopped. This checks entrypoints, not learning;
full-budget split training and the once-only full test attempt are complete;
one test trajectory failed. No training extension or test-driven tuning is authorized.
See [public reproduction evidence](release-reproduction.md).

RoboDuet now has implemented original-policy components, Go1/ARX5 runtime,
rewards/curricula, two-stage PPO, save/resume, and five-module export. Corrected
16-environment execution completed two updates and one resumed update, all exit
0; export consumption on saved real inputs is exact in both modes. This does
not demonstrate effective learning or identical physics trajectories after
resume. The separate [fixed physical evaluation](roboduet-evaluation-result.md)
retains first-failure 5 ms states and provides a CPU saved-state comparison video.
See [RoboDuet training](roboduet-training.md).

Current results: [all three fixed training seeds](umi-relaxed-seed-repetitions.md) have completed their 4000-iteration budgets, final MuJoCo/native evaluations and exported-package self-tests under body-speed-v3 and joint1000. Each uses 393,216,000 transitions. Final MuJoCo16 position errors are 10.169 / 12.300 / 12.499 mm, versus Official's 8.657 mm; all 48 final trajectories complete without inversion or numerical failure. Independent review found no new material integrity issue. This does not remove contact or broader stability limits: seed 1 has a sampled head contact of about 779 N in case 6, and its author-protocol latest500 includes two inverted early-terminated episodes. Author timeout fractions are 95.0% / 94.8% / 91.2%, versus Official's 97.8%. All random baselines, intermediate failures, nonfoot contact and protocol strata remain in the [combined report](../outputs/mujoco/umi_relaxed_body_speed_three_seed_summary/README.md); there is no checkpoint selection. The first UMI learning-and-fixed-policy-transfer loop is supported on the supplied trajectory pool, with contact, generalization and formal-acceptance limits explicitly retained.

2026-09-12. Public-API solver-only foot Fz reconstruction is integrated into the original EMD reward, and the newly exposed DOF/gyro velocity branches have been corrected on the independently validated Lab develop / Sim 6.1 combination. The earlier velocity-clamp-v2 4000-iteration chain completed with exit 0, but its final MuJoCo evaluation failed to establish stable transfer. The [historical learning report](umi-reconstructed-learning-result.md) separates completed training, native Lab behavior and independent-engine failures. Two earlier from-scratch training seeds completed their budgets and dual-engine evaluation using a normal-contact reward approximation; those learned results remain historical and do not validate the corrected input or subsequent physics fixes. No release, public push or hardware deployment has been performed.

That earlier velocity-clamp-v2 final MuJoCo evaluation attempted all 16 trajectories: 15 complete, including five inverted cases, and one BADQACC failure with an inverted prefix. On the common complete 15-case subset, iteration 500 to 4000 position/orientation means regress from 71.312 mm / 0.154385 rad to 359.468 mm / 0.559554 rad, and zero-ground-support samples rise from 3.691% to 23.997%. Native Lab author-style aggregate tracking and timeout fraction improve against the current official-policy reference, while timeout-only tracking is slightly worse and two final-policy episodes invert. Both Lab runs exited 0. Additional seeds of that failed configuration were not started. The same-target nominal full-duration Lab comparison completed all 16 cases for both policies with no inversion: official/new-4000 mean position errors are 10.080/12.476 mm, versus the severe MuJoCo failures. A subsequent single-variable Lab intervention relaxed the 18 native joint speed limits to 1000 rad/s: the official policy remained near 10 mm with no inversion, while new4000 worsened to 224.026 mm and 6/16 inversions. This supports dependence on that constraint, without explaining all MuJoCo differences.

A separate [1000-iteration weights-only adaptation](umi-relaxed-velocity-adaptation.md) was attempted to remove the observed dependence on joint-speed constraints. Its preflight passed, but the real chain exited 1 after 43 updates at an unsupported body-angular-velocity clamp branch; the 1000 endpoint was not reached. The captured finite state is retained. The missing source-supported body-speed bias has since been added, with independent formula review; full native 6D checks still have 10 and 1 out-of-tolerance components and near-zero EMD sensitivity remains. A separate fixed-1000 weights-only chain with this explicit limitation completed all 98,304,000 transitions with exit 0. Its MuJoCo evaluation completed all 16 cases without numerical failure but still inverted in cases3 and5; common-15 mean position/orientation improved to 256.539 mm / 0.309867 rad. Native nominal Lab completed 16 cases without inversion (13.907 mm), while author-style latest-500 tracking and timeout fraction remained worse than official (30.682 versus 20.076 mm; 89.2% versus 97.8%). The immediate stable-transfer objective is not met. Final export parity and a separate package-consumption case passed. The bounded two-case MuJoCo integration-step diagnostic completed and did not eliminate the failures; outcomes are nonmonotonic with finer integration. The adaptation log has plateaued, so it will not simply be extended. A separate fixed 4000 experiment from random initialization under the corrected physical variant started at 2026-09-11 23:09:08 UTC, with evaluation nodes 0/500/4000 and no checkpoint selection. It has now completed training and all final evaluation, as reported at the top of this page. Its random initializer and fresh optimizer/counters were verified, including identical independent CPU initializer tensors. The physical variant and original default remain distinct.

The [stock reconstruction report](umi-stock-solver-force.md) collects the formula, supported physics branches, numerical evidence and cross-version boundary. The live float32 shadow completed 3400 physical steps with exit 0. Independent CPU float64 recomputation found zero failures among 510000 acceleration components under the preselected tolerance. Current source semantics support the controlled original-EMD experiment; direct pointwise correspondence to old Gym GPU internals remains unverified. Release-environment integration passed a four-environment freshness check and one original 4096-environment PPO iteration with exit 0, 98304 transitions and finite checkpoint/metrics. The iteration took 2.892 seconds; sampled GPU-process peak memory was 5782 MiB.

The [isolated newer combination](../outputs/isaac/lab-develop-sim610-validation/result.md) subsequently passed upstream body-property application without PawCerto's compatibility loop, public dynamics/friction interfaces, three free-dynamics samples (450/450 components), reward freshness and original 4096-environment PPO integration. Independent review confirmed the finite checkpoint and 256 Adam updates. Fixed Lab `412fb31b30ee605b4ffec4327436fc0fe53281d8` / Sim 6.1.0.0 is now selected for the [preselected 4000-iteration experiment](umi-corrected-training-plan.md); latest release `v3.0.0-beta2.patch1` / Sim 6.0.1 remains fallback. Main code retains the per-link compatibility calls and explicitly sets the supported external-force branch. See [environment details](environment-versions.md).

The initial corrected main-code run received SIGTERM at iteration 29; sender is unknown and this does not establish an engine failure. Its original records and complete iteration-0 checkpoint are preserved. Recovery in `runs/umi_reconstructed_solver_seed0_4096_resume` completed 364 iterations (35,782,656 transitions), then exited 1 at a post-step gyroscopic velocity-domain guard during iteration 365. Its 364 metric rows are finite; no iteration-500 checkpoint exists. The failing condition is post-step angular speed above 1/dt = 200 rad/s, while this step's pre-state passed. This does not prove that its pre-state forward-dynamics gyroscopic clamp activated or that develop is incompatible. Subsequent native probes and source analysis corrected the PRE velocity handling, while retaining the numerical limits described below. That failed chain remains closed; the new runtime contract requires a fresh chain. See the [retained failure report](../runs/umi_reconstructed_solver_seed0_4096_resume/result.md). The [initial MuJoCo baseline](../outputs/mujoco/umi_reconstructed_solver_seed0_seed2027/model_0/README.md) completed all 16 cases with mean EE position/orientation errors 246.695 mm / 0.540687 rad, without inversion or numerical failure. This is random initialization. Final corrected-signal learned-control results are reported above; the failed earlier chain supplied no learned evaluation node.

A third migration correction now writes the original dimensionless friction coefficient through the actual public legacy setter. A training-mode initialization and one physical step verified all 18 mapped coefficients exactly and all new-model static/dynamic/viscous parameters as zero. See the [actual readback](../outputs/isaac/stock-legacy-friction-check/result.md). The three stock-property evaluations below preceded this friction repair and retain that boundary.

An isolated MuJoCo 3.13 comparison completed: the old iteration-4000 case-3 numerical reset did not recur, but the policy still inverted. A subsequent [48-case regression](../outputs/mujoco/version-3.13-regression/README.md) of the official policy and both selected historical policies completed without warning/reset/inversion, with small tracking differences and identical sampled support/contact counts. On that broader evidence, 3.13.0 is now the explicitly selected evaluation baseline for the next corrected training experiment. Both existing 3.8 environments remain unchanged. See [environment versions](environment-versions.md) and the [original failure-case comparison](../outputs/mujoco/version-3.13-check/README.md).

## Current remaining reconstruction limits

The complete DOF/gyro correction and 38 CPU checks are complete. Three original-limit native samples pass all 450 acceleration components in CPU64, CPU32 and actual CUDA32 under the original tolerance; actual CUDA maximum error/tolerance is 0.0309. The [report](../outputs/isaac/dof-clamp-free-diagnostic/cpu-production/README.md) and independent review retain both the corrected branches and their limits. The [high-speed native analysis](../outputs/isaac/gyro-clamp-free-diagnostic/cpu-production/README.md) retains full 6D FAIL: actual CUDA has three angular components over the original tolerance, while all 225 linear components pass. Near-zero foot residuals still alter the original EMD flying branch in those diagnostics; actual original Gym low-speed solver-free checks return exactly zero. This limits sensor/reward-equivalence claims and is not fixed by changing the tolerance.

A full active-physics configuration read identified omitted URDF DOF speed limits and contact/rest offsets. Actual native joint-speed readback now matches original Gym; robot contact/rest offsets now match 0.01/0 m, with the same values authored on the static ground. The [4096-environment check](../outputs/isaac/urdf-velocity-one-iteration/result.json) then stopped before its first PPO update at PRE calf velocity 20.0824 > 20.07 rad/s. The FD and public-C velocity-clamp rules differ; the complete bias/gyro correction now covers both while retaining original limits. A subsequent [actual 4096-environment iteration](../outputs/isaac/dof-clamp-one-iteration/validation.json) completed 98,304 transitions and 256 Adam updates with finite checkpoint/metrics. The fresh run is `runs/umi_reconstructed_solver_velocity_seed0_4096`, physics v3 / reconstruction v2, with fixed endpoint 4000 and evaluations at 0/500/4000. Current CUDA solver-free samples still expose the near-zero EMD limitation: one of three takes non-flying despite maximum foot residuals below 1e-6 N. An original Gym three-actor mass/COM check supported the existing inertia-scaling rule rather than identifying another defect.

## Earlier investigation and results

The user has made broad researcher usability a dependency constraint: the default path must use official, unmodified Isaac Lab/PhysX without an engine rebuild. The isolated native patch is retained as experimental evidence; no Lab override or custom bridge was started. Current work instead investigates public-API reconstruction. A real stock-library Go2+ARX5 capability capture exited 0 and returned finite floating-base M(24×24), g/C(24), CoM Jacobians(25×6×24) and link accelerations(25×6) over an actual 5ms step. The first delayed controller input was zero, so this only proves access, dimensions and finite values; nonzero-input reconstruction and original sensor equivalence remain unfinished. See [the stock probe](../outputs/isaac/stock-dynamics-capability-probe/result.md).

A subsequent [stock reconstruction capture](../outputs/isaac/stock-dynamics-reconstruction-capture/result.md) completed 12 physics steps with runtime and metadata validation exit 0. All 1,359 arrays are finite, including actual masses, full joint topology, paired dynamics and nonzero applied torques; every submitted torque matched the engine getter. External wrench inputs were zero. This supplies moving-state offline analysis without changing the engine or physics settings. Actual child-link body damping and velocity limits remain unverified through the captured public interfaces; reconstruction and original sensor equivalence are still under investigation.

That capture exposed a concrete configuration application defect: the installed Lab nested-property helper stops below the first rigid body, while this USD asset nests its links below base. Only base had received the requested PhysX rigid-body properties. PawCerto now explicitly applies the requested settings to each physical link through the official helper before simulation reset. A new capture in `outputs/isaac/stock-dynamics-all-body-properties-capture` exited 0 and verified the requested attributes on all 25 links, along with finite dynamics and exact submitted-input mapping. Independent review then found a second existing mismatch: Gym's body angular-speed limit is rad/s, whereas Lab's property interface is deg/s. The runtime now converts the original configured value with `math.degrees`; the [unit-corrected capture](../outputs/isaac/stock-dynamics-angular-unit-capture/result.md) independently verified all 25 USD limits as 57295.78125 deg/s, the float32 representation of the original 1000 rad/s target, and exited 0. The intermediate capture's maximum recorded angular speed was below its erroneous limit, so no effect of that limit on those 12 steps was demonstrated.

Earlier captures and trained-model evaluations remain evidence for the earlier configuration. The fixed-weight author-style regression under corrected properties has now completed for the official policy and both trained seeds, each with exit 0 and the latest 500 completed episodes. Official/seed0/seed1 EE means are 22.175/26.794/26.218 mm and timeout survival proxies are 96.8/91.4/88.8%; changes from the earlier configuration are mixed, not a consistent improvement. All summary sums/means were recomputed from the retained records. See the [complete regression readout](../outputs/isaac/author-validation-stock-properties-seed2027/readout.md). The protocol and trajectory pool are unchanged, but the episodes are not paired; no new training or MuJoCo rerun occurred.

The earlier 12-step [offline model residual](../outputs/analysis/stock-solver-force-probe.md) is conditional on unverified child properties and is not an original sensor measurement. Subsequent captures and their offline analyses retain separate identities. The [unit-corrected model calculation](../outputs/analysis/stock-solver-force-probe-angular-unit.md) passed velocity-mapping and linear-solve checks; the saved states do not trigger the source DOF-speed or gyro clamps. A subsequent [full-robot free-dynamics check](../outputs/isaac/stock-free-dynamics-check/result.md) completed exactly three independent 5ms steps under diagnostic conditions excluding active solver constraints, with unchanged robot mass and inertia. All 450 acceleration elements passed the preselected tolerance; maximum linear/angular errors were 2.72216e-6 m/s² and 1.96961e-5 rad/s². The official GPU run exited 0, and setup failures are retained separately. This validates those stock free-dynamics samples; contact-state reconstruction and original Gym sensor equivalence remain separate checks. A GPU diagnostic implementation and full contact rollout are now in progress without changing the reward input.

An upstream check on September 12 located the same nested-property defect in [Isaac Lab PR #6377](https://github.com/isaac-sim/IsaacLab/pull/6377), merged into `develop` on July 17. It lets the rigid-body and mass-property writers descend below successful parent bodies. The installed `v3.0.0-beta2.patch1` tag predates that change. PawCerto's explicit per-link calls address the defect without modifying the installed Lab package; this should be revisited when validating an upstream version that contains the fix.

## Executed

- Downloaded the official UMI source/assets and published data/checkpoints; both ZIP archives passed CRC checks.
- Loaded the complete Go2 + ARX5 in MuJoCo: 18 joints, 41 source links, 18.879 kg. Independent FK over 32 random configurations matched to 8.88e-16, with source inertias retained.
- Executed the original Isaac Gym asset query and confirmed FL → FR → RL → RR → arm joint ordering.
- Compared the shared UMI observation, actor and PD against the original implementation on CPU, including constructor field ordering and history.
- Ran the released tossing `ours` and `ours-real` checkpoints in independent MuJoCo for one complete 17-second trajectory each. Mean EE position error: 8.92 mm / 5.68 mm; mean orientation error: 0.0291 / 0.0221 rad. Both remained upright. Detailed outputs are under `outputs/mujoco/`.
- Executed the original Isaac Gym released policy for the full 17-second task. The [reference video](/home/lyb/PawCerto/outputs/videos/umi_original_isaacgym.mp4), 1600×912 at 24 fps, passed full video decoding. This shows the official model, not a newly trained PawCerto policy.
- Executed the corrected Isaac Lab asset and control path. The importer-created 1 kg mass on the zero-mass EE marker was removed; corrected measured mass is 18.8789996 kg, with 34 collision shapes and explicit capsule replacements.
- Ran the original PPO from official actor/critic initialization for 4096 environments × 24 steps × 100 iterations: 9,830,400 transitions. Retained the 17-second task, reward formulas, curriculum, randomization and 64 epochs / 4 minibatches, with the subsequently confirmed foot-force input difference described below. Exported five saved candidates and evaluated each on the same 16 full trajectories in both engines.
- Restored the actual checkpoint at iteration 100 and trained through 101. Optimizer, RNG and curriculum restoration were exercised; physics resets on resume, so this does not reproduce an uninterrupted simulation trajectory.
- All 25 integrated CPU tests passed after the dense-evaluation, contact-reading, numerical-failure and plotting changes, including a real induced MuJoCo auto-reset and rejection of invalid prefixes in full-case plots. Source/asset/state/control/PPO/resume checks also remain green. They do not establish learned control quality.

## Pilot result

**The adaptation Pilot did not show overall control improvement.** Final versus initial mean position error was 9.433 versus 9.397 mm in Isaac Lab and 8.890 versus 8.560 mm in MuJoCo. Orientation error rose 30.29% and 33.60%, respectively. All five candidates retained support without observed inversion in nominal cases.

With the original randomization, 5-second pushes and 7-second transports, both initial and final weights had 10/16 inverted cases. Position RMS rose 8.49%; slightly better orientation and support do not establish overall robustness gains. These cases use available training trajectories, not an independent holdout. See the [Pilot report and plots](umi-transfer-pilot.md) for definitions, candidate comparisons and raw links.

## Historical sensor investigation and earlier training

The following entries retain the sequence before the current solver-force and
body-speed-v3 results. Statements of unfinished work in these entries describe
that historical stage; the current status and remaining limits are above.

A subsequent real original-Isaac-Gym probe confirmed a reward-input mismatch in the completed training runs. The port supplies foot normal-contact Fz to `EvenMassDistribution`; the original method uses its foot force-sensor Fz. During a 19-sample diagnostic with all four feet at least 1.955 m above the ground and every body contact force zero, the original sensor remained nonzero and 15 samples changed the reward's flying classification when replaced by contact force. The normal 3400-sample rollout also showed different forces. The verified probe exited 0 after correcting probe-only startup/cleanup issues; its raw arrays exactly matched the earlier saved capture. Outputs are in `outputs/isaac/original-foot-sensor-probe-verified`.

This input difference affects the active mass-distribution reward; the actor and critic do not directly observe the force sensor. It does not by itself explain the measured performance gap. Lab incoming-joint-force and summed-contact candidates did not establish equivalence. A subsequent original-Gym single-step comparison also failed its prerequisite: the supposedly unconstrained copy still reported solver force despite zero contact and widened joint limits. That reconstruction was rejected without fitting coefficients. No inferred wrench formula was connected to training. The current normal-contact approximation is retained explicitly; see the [sensor investigation](umi-force-sensor-consumers.md). Contact-based ground-support diagnostics and collision termination remain separate from the reward signal.

A subsequent native-interface check found that PhysX removed `PxArticulationSensor`, its flags and sensor readout APIs starting in version 5.4, with incoming joint forces as the replacement. The installed schema validator also identifies the old force-sensor schema as removed. Although a C++ articulation-pointer entry exists, it does not restore the removed sensor API or its original flags. Thus the current version has no verified direct route to enable the old signal; this read-only finding is not a runtime repair. Sources are linked in the sensor investigation.

An actual isolated SDK configuration attempt at the fixed public PhysX commit is recorded in [the bounded build probe](../third_party/physx-sensor-probe/probe-result.md). A supported existing-Python override progressed past the first bootstrap failure, but the next metadata package failed DNS resolution; the wrapper then exited 1 with a secondary missing-environment error. No CMake/compiler or replacement library was produced. The current source does contain an internal solver-delta-velocity accumulator, but exposing it with correct timing and validating original sensor semantics remain unfinished. The existing Isaac installation and training were not modified.

A four-file [experimental raw solver-delta patch](../patches/README.md) extends the existing DirectGPU read route. On resuming work, the official standalone SDK CMake entry configured successfully with installed GCC/CUDA/CMake and snippets/PVD runtime disabled; it does not need the metadata dependency that blocked the earlier Packman route. The patched host/CUDA target, complete SDK and separate probe have built and linked, after a version-guarded CUDA 13 context-call compatibility correction. The [actual GPU probe](../third_party/physx-sensor-probe/readout-probe/runtime-supported-checks.log) passed 1600 finite active-vector checks, 1920 reversed-index comparisons and 320 padding-NaN checks at 5 ms. This establishes basic raw-readout availability, not original force-sensor equivalence or Lab integration. Its attempted sleep phase was rejected: supported DirectGPU scenes force sleeping off. The earlier static all-sleep stale-count analysis omitted this reachability restriction, so its runtime-P1 classification is withdrawn; the revised count source remains a static precaution. A Lab override was considered but not started; that integration is now paused in favor of stock public APIs. The patch is not installed or connected to the reward. A command-scoped CDN address override also returned HTTP 200 for the original official metadata URL with normal TLS verification. DNS is no longer the immediate minimum-SDK build blocker; earlier failed attempts remain recorded.

An independent from-scratch seed-1 run in `runs/umi_lab_scratch_seed1_4096` completed its preselected 3500 iterations and 344,064,000 transitions, saved `model_3500.pt`, and exited 0. It used no pretrained weights or resume and retained the same 4096×24 / 64-epoch / 4-minibatch / 17-second recipe. Its continuous finite training log records 5652.75 seconds (94.21 minutes); checkpoint configuration, optimizer steps and tensor finiteness were verified on CPU. It tests repeatability of the documented port, not a sensor correction or strict original-method equivalence. CPU checks at 0/500/3500 and final author-style evaluation completed with the already fixed evaluation seed 2027. The existing seed-0 candidate was selected after comparing its checkpoints; this selection difference remains explicit in the comparison.

Seed 1's final MuJoCo evaluation completed 16/16 full 17-second cases without inversion or numerical failure. Position/orientation means fell from initialization's 254.349 mm / 0.574514 rad to 15.782 mm / 0.028885 rad, improving both in all 16 paired cases. The intermediate 500 node had 43.116 mm and two inverted cases. Final versus 500 position improved in only 7/16 cases despite the lower group mean; removing severe failures matters to that aggregate. Final ground-supported feet averaged 3.9199, zero-ground-foot samples were 0.0663%, and nonfoot contacts carried 0.282934% of sampled upward ground force. These are support diagnostics, not contact-free or continuous-time guarantees.

Seed 1's final author-style Lab evaluation exited 0 after 502 completed episodes, retaining the latest 500 as prescribed. Mean position/orientation errors were 27.035 mm / 0.066884 rad, the timeout survival proxy was 89.8%, and no included episode inverted. Seed 0's selected 3500 yielded 26.370 mm / 89.4%; official weights yielded 20.838 mm / 97.2%. Final quality thus roughly repeats seed 0 while remaining below the official reference. All 500-episode error means include early-termination prefixes; timeout survival is not task success. These are samples from the same supplied trajectory pool, not a holdout. The [independent-seed report](umi-repeated-seeds.md) retains complete training, behavior and selection boundaries.

The [seed-1 final export](../outputs/export/umi_lab_scratch_seed1_3500/README.md) passed zero-error actor reload parity and direct 17-second MuJoCo package consumption, with 11.563 mm mean position error and no inversion or numerical failure. That single deployment case differs from the 16-case validation draw and does not replace it.

From-scratch training began in `runs/umi_lab_scratch_4096`, using the official `ours/config.json` starting recipe without pretrained weights: 17-second task, eight previews, actor 132 / critic 261 inputs, 4096 environments × 24 steps, 64 PPO epochs / 4 minibatches. The starting recipe was checked against the official saved configuration and its save-before-environment-construction code path. Current repository Hydra defaults describe a different recipe.

The original process ended by SIGTERM after iteration 2114, without a traceback; the signal's source is unknown. Training resumed from the last complete iteration-2000 checkpoint in `runs/umi_lab_scratch_4096_resume` and finished iteration 4000 with exit 0. Optimizer, RNG and curriculum resumed; physics reset. The effective weight chain contains 393,216,000 transitions and 6576.99 seconds of logged training time. Including the original 114-iteration unsaved tail, actual recorded use is 404,422,656 transitions and 6763.12 seconds. The tail remains in the data and resource accounting but is excluded from the recovered weight lineage.

The first 4,000-iteration stage is complete with checkpoints every 500. The paper evaluates a checkpoint at 4000; the saved recipe's 20,000-iteration limit is not a requirement to continue regardless of results. A dedicated worker owns GPU execution, while independent CPU MuJoCo evaluation and training readout are delegated. All saved intermediate checkpoints were evaluated in an exploratory model-selection extension, preserving the original preselected comparison. Official weights and scratch 500/3500 were then evaluated with seed 2027, fixed before author-3500 selection results were available.

The [checkpoint selection and validation report](umi-checkpoint-selection.md) retains every candidate and both evaluation seeds. In new-seed MuJoCo, official/500/3500 each completed 16 full cases without inversion or numerical failure; mean position errors were 8.657 / 17.861 / 14.585 mm. In the new-seed author-style Lab evaluation, errors were 20.838 / 31.707 / 26.370 mm, timeout survival proxies 97.2% / 82.2% / 89.4%, and inversion counts 1 / 3 / 1 per 500 completed episodes. Iteration 3500 retains better tracking than 500, with more zero-ground-foot samples. This is new sampling from the same training pool, not a trajectory holdout or independent training-seed replication.

A [saved-record coordination readout](../outputs/mujoco/umi_validation_seed2027/coordination_readout.md) reconstructed foot sphere positions with CPU kinematics for the existing official/3500 cases, without new simulation. Both show base motion and foot clearance after startup. Official/3500 median base XY net displacements are 0.071/0.244 m and sampled path lengths 0.394/0.778 m; median maximum attitude changes are 11.6/29.0 degrees. The learned policy's greater body movement accompanies higher tracking error and does not prove better or necessary coordination. The 20 ms records cannot distinguish continuous contact, rolling and sliding between saved endpoints.

The [evaluation conditions](umi-evaluation.md) distinguish 16-case diagnostics from checkpoint-config evaluation using author episode semantics, seed 2026 and a nominal 500 completed episodes. The published tossing pool has no supplied holdout split; this limitation is retained explicitly.

The random-policy MuJoCo baseline completed the same 16 full trajectories: mean EE error 0.2401 m, mean orientation error 0.5232 rad, four supporting feet on average and no inversion. At iteration 500, the same paired test reached 0.01664 m and 0.04958 rad, with both errors improving in all 16 cases and no inversion. Mean support was 3.9335 feet; one of 13,584 recorded time points had no foot contact above 1 N. This is real from-scratch tracking improvement in independent MuJoCo on training trajectories. It does not complete the original-method reproduction. Results are under `outputs/mujoco/umi_lab_scratch/`.

Later checkpoints regressed in the same 16-case MuJoCo evaluation: iteration 1000 had mean position/orientation errors 33.80 mm / 0.13909 rad and 1 inversion; iteration 2000 had 82.55 mm / 0.18355 rad and 2 inversions. Ground-filtered contact auditing distinguished foot contact with the floor from the earlier net-foot-force proxy. At iteration 500, calf contacts carried only 0.166% of sampled upward ground force across cases; this does not support a claim that the robot mainly rested on its calves. At iteration 2000, the corresponding fraction was 7.90%, and zero-ground-foot samples occurred more often than the old net-force proxy indicated. The [stage report](umi-scratch-stage1.md) retains the complete results and failure traces.

The new `scripts/eval_umi.py` entry has passed independent source review and actual GPU execution with the preselected official `ours`, scratch 500 and scratch 4000 weights. Over the last 500 completed episodes, mean position errors are 19.18 / 31.54 / 25.53 mm, orientation errors 0.05703 / 0.09956 / 0.06578 rad, and timeout survival proxies 98.2% / 84.2% / 87.8%. Inverted episode counts are 0 / 1 / 1. Ground-only contact reporting was exercised. These are results under the documented checkpoint-config evaluation conditions, not proof of exact paper or numerical engine equivalence; early-termination prefixes and full timeouts are also reported separately.

The 16-case Lab nominal comparison shows learning from initialization through iterations 500/1000/2000/4000: position error 313.66 / 18.90 / 12.57 / 12.71 / 10.90 mm, with no inversion. In the paired original-randomization/push/transport diagnostic, corresponding errors are 707.1 / 294.6 / 209.5 / 155.9 / 226.5 mm, with 13 / 12 / 6 / 6 / 7 inverted cases. Stronger nominal Lab tracking does not establish better perturbation behavior or MuJoCo transfer.

Iteration 4000's MuJoCo case 3 exposed a runtime reporting bug: BADQACC triggered the engine's automatic reset at 16.88 seconds, and checking only finite final positions missed it. Failure propagation was corrected and a targeted repeat reproduced the failure with the same 843-record prefix and exit 2. Reset-after-failure samples are excluded; the original output remains preserved. This node has 16 attempts, 15 complete cases and one numerically invalid case; two of the complete cases inverted. The 15-case means are separate from whole-group metrics. Earlier 0/500/1000/2000 traces were checked for time resets without unnecessary reruns. The [stage report](umi-scratch-stage1.md) gives the qualifying details.

Deployment packages for iterations 500, 3500 and 4000 have each executed a full 17-second MuJoCo case directly from the exported actor/config/joint names. The 3500 package's actual run had mean position error 11.515 mm, orientation error 0.045727 rad and no inversion. A [CPU-rendered comparison video](../outputs/videos/scratch3500_vs_official_case0.mp4) shows preselected `sample(16,0)` case 0 for iteration 3500 and the official model; the package command uses `sample(1,0)`, a different draw. These single-case executions and saved-state replay do not replace the complete evaluation groups. The verified policy joint order is now included in `configs/umi_go2_arx5_joint_names.json`, and the default export entry was actually exercised with zero actor reload error.

Earlier MuJoCo tumble results used an incorrectly ordered observation vector. The adapter and source-comparison test were corrected, and the original reset warmup was matched. Those earlier diagnostics are retained separately and are not evidence against UMI-on-Legs.

Earlier Lab results with the extra marker mass are also retained as diagnostics. Measured geometry/control parity and collision properties do not establish complete numerical equivalence between engines. Randomized inertia and internal shape ordering have not been exhaustively verified over all parameters. Neither the Pilot nor current from-scratch progress establishes stable learned WBC or hardware readiness.

Read-only preparation for later methods is complete in the [RoboDuet reference check](roboduet-reference.md) and [DeepWBC reference check](deepwbc-reference.md). Both have source asset references but no policy package was found in the inspected official public entries. DeepWBC's default configuration additionally enables position tracking with only three nonzero arm action scales, not arbitrary 6D pose tracking. This was the earlier reference-only stage; current RoboDuet implementation and actual 0/1600 physical results are reported above. See the current DeepWBC status above for its separate evidence level.

The bounded [MLM material check](mlm-reference.md) identified the exact paper and its described mechanisms, but did not locate an official complete repository, policy weights, matched Go2 + Airbot Play asset, full task trajectories or AMP reference data in the inspected public entries. Related FastUMI Pro examples are not established as the MLM training set. These missing materials remain a later-method limitation and do not block UMI work.

## KMA development

The installed 0.2.8 Skill was available and read, but this task still required user corrections to delegation. The testing documentation's narrower trigger wording and the instruction that Master “handles shared resources” were concrete defects/ambiguities. A local candidate aligns trigger wording, separates resource coordination from the assigned operator, and moves existing task-partitioning guidance into the action-selection section. Existing deterministic tests passed without changing evaluation criteria.

The development plugin was first installed locally as `0.2.8+codex.20260911062528`. A subsequent independent KMA review found the same outdated trigger wording still present in both FAQs. Those two paragraphs are now aligned with the active Skill and TESTING guidance; no new decision rule or mandatory workflow was added. The candidate was reinstalled as `0.2.8+codex.20260911170511`; all eight changed source files match the installed cache. Static validation, 45 unit tests, all 16 documentation pages and whitespace validation passed, and the reviewer closed the FAQ finding.

Active agents explicitly adopted the existing guidance in this PawCerto task. Testing guidance reflects the user's requested direct development-version dogfooding; a separate isolated behavioral task was stopped and is not counted as successful validation. The later review also identified treating old-GPU pointwise equality as a prerequisite as an application error: existing experiment guidance already says a self-imposed diagnostic condition does not automatically become a prerequisite. The response was to return to the source-supported reward experiment, not add another rule. Native role execution and current-task adoption have been observed, but broad KMA reliability and measured productivity improvement have not been established. The FAQ update changes documentation, not the loaded Skill or roles, and reinstall verification does not establish fresh-session automatic loading. No public KMA version was released.

The user subsequently identified redundant Master progress polling while the GPU worker still owned the full running task. This preceded the later account-limit failures, so those failures do not explain all of the duplication. Existing guidance already assigns complete execution to the worker and tells Master to use its findings; this is an observed application failure, not evidence that another mandatory rule is needed. The operational correction is to receive worker milestones and investigate only a meaningful evidence gap or exception while delegation is available. The user clarified that exhaustion of the weekly account quota and the subsequent takeover are outside the KMA issue under investigation. The relevant behavior is redundant Master polling while the assigned worker was available; the next useful verification concerns application of the existing delegation boundary in real work. The candidate's behavioral effectiveness remains unproven despite passing static tests.

The renewed effectiveness investigation used fresh native KMA contexts for activation/ownership review, guidance implementation, GPU execution and CPU delivery. The read-only audit confirmed actual role-instruction propagation and current Skill adoption, while distinguishing a stale Skill path advertised by the old conversation from fresh-session discovery. The actionable ownership gap was repaired in four existing guidance surfaces: complete delegation explicitly includes execution, observation and routine recovery within scope until a result or boundary is reported; Master advances other goals or awaits meaningful results while the worker progresses normally. No new stage, role, test gate or telemetry was added. Existing validation, 18 setup unit tests, both Skill validators and whitespace checks passed. The local candidate was installed as `0.2.8+codex.20260912024302`; all eight changed candidate files match the installed cache, and only the existing PawCerto managed instruction block was refreshed.

The GPU and CPU owners now exchange checkpoint/result notifications directly. While they retain the full two-seed completion scope, Master integrated the KMA change instead of mirroring training progress. The CPU owner independently repaired inherited export-report constants and completed the current-result-first README without a fresh simulation. A fresh read-only Codex session in this project completed its review of the real export and three-seed tools with actual exit 0, using a task prompt that did not mention KMA or prescribe delegation. Its main agent automatically read the current Skill, selected a native `kiss_reviewer`, and that child also read the current Skill. The review found three material defects: stale successful outputs could pass after a failed export self-test process; native aggregation expected 850 records despite one unsaved warmup and 849 saved endpoints; and progress plots could compare different case denominators after an invalid case. The CPU owner fixed all three, and offline probes rejected the stale-output failure, accepted the existing 849-record native clocks, and confirmed the paired Official baseline after excluding a case. Master inspected the affected branches without repeating physical evaluation. This demonstrates fresh main/child adoption and a useful autonomous review on this bounded real task; sustained effectiveness and measured productivity gains remain unestablished. No public KMA version was released.

Later, Master waited for seed 2 while seed-1 final review and the user-entry review could already proceed. The user had to point this out; the earlier successful fresh-session review therefore did not establish sustained useful scheduling. The next narrow repair replaces the ambiguous choice between advancing other goals and waiting: advance or delegate work whose own dependencies are ready, including independent parts of unfinished goals; wait only when no useful work is ready. The four existing guidance surfaces were synchronized, existing validation and 18 setup tests passed, and the candidate was installed as `0.2.8+codex.20260912051546`, with all eight changed source files matching the cache. Native seed-1 review completed without a new integrity finding. A fresh native entry reviewer found the README's missing parent-directory creation and the incomplete fresh-install route; the directory error was fixed and exercised, while a separate fresh native coder owns isolated installation reproducibility. Those reviews began after the user's correction and before the new text was installed, so their benefit is not evidence caused by that text change. A fresh real report-writing session, prompted without naming KMA or prescribing delegation, automatically loaded the updated Skill, wrote the available report sections while seed 2 was pending, then incorporated the actual final three-seed/native results and completed its assigned report with exit 0. It did not take over simulation or repeat raw integrity checks. This provides a bounded example of useful work proceeding before a final dependency resolves, as well as complete scope retention; it does not establish long-term scheduling reliability or measured productivity gains.

A final fresh native KMA reviewer found no additional actionable conflict in the updated guide and recommended stopping rule additions. Its independent scope covered guide text and inference boundaries; runtime facts were supplied in the handoff, so it is not counted as another independent runtime validation. Two separate fresh UMI analyses contributed the next-step proposal: saved event ordering and original-method constraint consumers. They found an actionable missing 5 ms observation window, not evidence for adding broad rewards or guards. No follow-on training or diagnostic rollout was run for that proposal.
